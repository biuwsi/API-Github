spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false

eureka:
  enabled: false

github:
  templates:
    - type: MICROSERVICE
      url: https://github.com/biuwsi/TEMPLATE-Microservice
      temp-folder: microservice-template